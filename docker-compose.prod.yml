# =============================================================================
# AI Exam Engine — Docker Compose (Production using Neon & Upstash)
# =============================================================================
# Services:
#   backend             — Django/DRF Gunicorn API Server (Port 9000 on host)
#   summary_worker      — Celery worker (Low/Normal priority tasks)
#   roadmap_worker      — Celery worker (Roadmaps & Maintenance)
#   pdf_worker          — Celery worker (High priority PDF parsing)
#   analytics_worker    — Celery worker (Low priority analytics/leaderboards)
#   notification_worker — Celery worker (High priority emails/notifications)
#   beat                — Celery beat periodic scheduler
#   flower              — Celery monitoring dashboard (Port 5555)
#   frontend            — Next.js production build server (Port 3000)
# =============================================================================

version: '3.8'

services:
  # ─── Backend (Django API) ───────────────────────────────────────────
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: /app/entrypoint.sh gunicorn core.wsgi:application --bind 0.0.0.0:8000 --workers 3
    volumes:
      - /var/www/examintel/media:/app/media
      - django_prod_static:/app/staticfiles
    ports:
      - "127.0.0.1:9000:8000" # Exposed to Nginx reverse proxy (host port 9000)
    env_file:
      - ./backend/.env
    environment:
      - RUNNING_IN_DOCKER=true
      - DEBUG=False
      - REDIS_URL=redis://redis:6379/0
      - REDIS_CACHE_URL=rediss://default:gQAAAAAAAfYCAAIgcDFhNjQ3M2Q3NTNiOTg0YTNhYjc1YzU0NTUzNDViMGY3MA@fluent-koi-128514.upstash.io:6379
      - REDIS_SESSION_URL=rediss://default:gQAAAAAAAfYCAAIgcDFhNjQ3M2Q3NTNiOTg0YTNhYjc1YzU0NTUzNDViMGY3MA@fluent-koi-128514.upstash.io:6379
    depends_on:
      - redis
    restart: always

  # ─── Celery Workers ─────────────────────────────────────────────────
  summary_worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: celery -A core worker -Q normal,low -n summary_worker@%h --loglevel=info --concurrency=2 --without-gossip --without-mingle --without-heartbeat
    volumes:
      - /var/www/examintel/media:/app/media
    env_file:
      - ./backend/.env
    environment:
      - RUNNING_IN_DOCKER=true
      - DEBUG=False
      - REDIS_URL=redis://redis:6379/0
      - REDIS_CACHE_URL=rediss://default:gQAAAAAAAfYCAAIgcDFhNjQ3M2Q3NTNiOTg0YTNhYjc1YzU0NTUzNDViMGY3MA@fluent-koi-128514.upstash.io:6379
      - REDIS_SESSION_URL=rediss://default:gQAAAAAAAfYCAAIgcDFhNjQ3M2Q3NTNiOTg0YTNhYjc1YzU0NTUzNDViMGY3MA@fluent-koi-128514.upstash.io:6379
    depends_on:
      - backend
      - redis
    restart: always

  roadmap_worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: celery -A core worker -Q normal,maintenance -n roadmap_worker@%h --loglevel=info --pool=threads --concurrency=4 --without-gossip --without-mingle --without-heartbeat
    volumes:
      - /var/www/examintel/media:/app/media
    env_file:
      - ./backend/.env
    environment:
      - RUNNING_IN_DOCKER=true
      - DEBUG=False
      - REDIS_URL=redis://redis:6379/0
      - REDIS_CACHE_URL=rediss://default:gQAAAAAAAfYCAAIgcDFhNjQ3M2Q3NTNiOTg0YTNhYjc1YzU0NTUzNDViMGY3MA@fluent-koi-128514.upstash.io:6379
      - REDIS_SESSION_URL=rediss://default:gQAAAAAAAfYCAAIgcDFhNjQ3M2Q3NTNiOTg0YTNhYjc1YzU0NTUzNDViMGY3MA@fluent-koi-128514.upstash.io:6379
    depends_on:
      - backend
      - redis
    restart: always

  pdf_worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: celery -A core worker -Q high,normal -n pdf_worker@%h --loglevel=info --pool=threads --concurrency=4 --without-gossip --without-mingle --without-heartbeat
    volumes:
      - /var/www/examintel/media:/app/media
    env_file:
      - ./backend/.env
    environment:
      - RUNNING_IN_DOCKER=true
      - DEBUG=False
      - REDIS_URL=redis://redis:6379/0
      - REDIS_CACHE_URL=rediss://default:gQAAAAAAAfYCAAIgcDFhNjQ3M2Q3NTNiOTg0YTNhYjc1YzU0NTUzNDViMGY3MA@fluent-koi-128514.upstash.io:6379
      - REDIS_SESSION_URL=rediss://default:gQAAAAAAAfYCAAIgcDFhNjQ3M2Q3NTNiOTg0YTNhYjc1YzU0NTUzNDViMGY3MA@fluent-koi-128514.upstash.io:6379
    depends_on:
      - backend
      - redis
    restart: always

  analytics_worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: celery -A core worker -Q low,maintenance -n analytics_worker@%h --loglevel=info --pool=threads --concurrency=4 --without-gossip --without-mingle --without-heartbeat
    volumes:
      - /var/www/examintel/media:/app/media
    env_file:
      - ./backend/.env
    environment:
      - RUNNING_IN_DOCKER=true
      - DEBUG=False
      - REDIS_URL=redis://redis:6379/0
      - REDIS_CACHE_URL=rediss://default:gQAAAAAAAfYCAAIgcDFhNjQ3M2Q3NTNiOTg0YTNhYjc1YzU0NTUzNDViMGY3MA@fluent-koi-128514.upstash.io:6379
      - REDIS_SESSION_URL=rediss://default:gQAAAAAAAfYCAAIgcDFhNjQ3M2Q3NTNiOTg0YTNhYjc1YzU0NTUzNDViMGY3MA@fluent-koi-128514.upstash.io:6379
    depends_on:
      - backend
      - redis
    restart: always

  notification_worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: celery -A core worker -Q high -n notification_worker@%h --loglevel=info --pool=threads --concurrency=4 --without-gossip --without-mingle --without-heartbeat
    volumes:
      - /var/www/examintel/media:/app/media
    env_file:
      - ./backend/.env
    environment:
      - RUNNING_IN_DOCKER=true
      - DEBUG=False
      - REDIS_URL=redis://redis:6379/0
      - REDIS_CACHE_URL=rediss://default:gQAAAAAAAfYCAAIgcDFhNjQ3M2Q3NTNiOTg0YTNhYjc1YzU0NTUzNDViMGY3MA@fluent-koi-128514.upstash.io:6379
      - REDIS_SESSION_URL=rediss://default:gQAAAAAAAfYCAAIgcDFhNjQ3M2Q3NTNiOTg0YTNhYjc1YzU0NTUzNDViMGY3MA@fluent-koi-128514.upstash.io:6379
    depends_on:
      - backend
      - redis
    restart: always

  # ─── Celery Beat (Scheduler) ───────────────────────────────────────
  beat:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: celery -A core beat --loglevel=info --scheduler django_celery_beat.schedulers:DatabaseScheduler
    volumes:
      - /var/www/examintel/media:/app/media
    env_file:
      - ./backend/.env
    environment:
      - RUNNING_IN_DOCKER=true
      - DEBUG=False
      - REDIS_URL=redis://redis:6379/0
      - REDIS_CACHE_URL=rediss://default:gQAAAAAAAfYCAAIgcDFhNjQ3M2Q3NTNiOTg0YTNhYjc1YzU0NTUzNDViMGY3MA@fluent-koi-128514.upstash.io:6379
      - REDIS_SESSION_URL=rediss://default:gQAAAAAAAfYCAAIgcDFhNjQ3M2Q3NTNiOTg0YTNhYjc1YzU0NTUzNDViMGY3MA@fluent-koi-128514.upstash.io:6379
    depends_on:
      - backend
      - redis
    restart: always

  # ─── Flower (Celery Monitoring) ────────────────────────────────────
  flower:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: celery -A core flower --port=5555 --basic_auth=admin:Admin@examintel123
    ports:
      - "127.0.0.1:5555:5555" # Bound to localhost for security
    env_file:
      - ./backend/.env
    environment:
      - RUNNING_IN_DOCKER=true
      - REDIS_URL=redis://redis:6379/0
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
    depends_on:
      - backend
      - redis
    restart: always

  # ─── Frontend (Next.js) ────────────────────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
      args:
        - NEXT_PUBLIC_API_BASE_URL=https://api.examintel.in/api
    ports:
      - "127.0.0.1:4005:4005" # Exposed to Nginx reverse proxy on port 4005
    environment:
      - NODE_ENV=production
    depends_on:
      - backend
    restart: always

  # ─── Redis (Local Cache, Sessions & Queue Broker) ───────────────────
  redis:
    image: redis:7-alpine
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    restart: always

volumes:
  django_prod_static:
  redis_data:
